# <卡号> · <一句话标题>

> **自包含标准**:另开一个全新会话的 agent,只读这张卡就能把活干完并通过验收。
> 卡号用 `W01`、`W02`… 递增(派工卡编号,与知识库的 P/T 编号无关)。

## 目标

<做完之后世界有什么不同,一两句>

## 背景 / 已有的东西

<相关现状:涉及哪些 projects/tasks/assets、走到哪一步、为什么要做这个;文件路径列全>

## 怎么做

<实现路线,关键步骤列点;有明确取舍就写死,不给施工方发挥空间>

## 文件归属(worktree 隔离)

- 允许改:<明确到文件/目录>
- 禁止碰:除上面以外的一切,尤其真相源/协作文档/`assets/codes.md`/`scripts/*.sh`(见 WORKER.md 铁律 2)

## 怎么验收

- [ ] <可执行的硬标准,逐条列>
- [ ] 改了脚本则 `bash -n` 过;改了 frontmatter 则 YAML 合法字段齐

## 退路

<做不下去/做错了怎么撤:revert 哪个 commit、删哪个分支/worktree>
